# 航向角标定
# 起飞前将无人机机头对准任务方向, 记录此时遥控器/地面站显示的航向角作为 measured
# reference_heading 为该方向在地理坐标系中的标准航向角
# 系统将自动计算 yaw_error = measured - reference, 并修正 initYaw_
yaw_calibration:
  reference_heading: 188       # 标准航向角 (deg, 0=正北)
